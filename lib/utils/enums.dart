enum UserType {
  user,
  vendor,
}

enum PostType {
  image,
  video,
  audio,
  pdf,
}

enum SwitchValue {
  phone,
  email,
  none,
}

enum MediaPathType { file, network, asset }

enum FileType {
  any,
  media,
  image,
  video,
  audio,
  custom,
}

enum CallingType { fetch, categories, brands, products, search }
